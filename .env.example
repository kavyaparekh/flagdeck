# Vercel KV / Redis (from the Vercel Storage tab — Redis integration)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Shared passcode for the admin UI (not real auth — see README)
NEXT_PUBLIC_ACCESS_PASSCODE=
